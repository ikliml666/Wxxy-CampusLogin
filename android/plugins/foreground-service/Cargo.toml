[package]
name = "tauri-plugin-campus-monitor-service"
version = "0.1.0"
description = "前台服务保活(通知+WifiLock+WakeLock)+开机自启,业务逻辑全在 Rust 循环"
edition = "2021"
links = "tauri-plugin-campus-monitor-service"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
serde = "1"
serde_json = "1"
tauri = { version = "2" }
